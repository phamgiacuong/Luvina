package luvina.service;

import luvina.model.AccTransaction;

public interface AccTransactionService {
    void save(AccTransaction accTransaction);

//    AccTransaction findAccId(Integer accountId);
}
